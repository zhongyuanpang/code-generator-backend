<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pzy.codegenerator.connect.mapper.CommonMapper">

    <select id="getAllTable" resultType="map">
        SELECT TABLE_NAME, TABLE_COMMENT FROM `information_schema`.`tables`
        <trim prefix="where" prefixOverrides="and | or">
            <if test="query != null and query != ''">
                (
                TABLE_NAME like concat('%',#{query},'%')
                or TABLE_COMMENT like concat('%',#{query},'%')
                )
            </if>
        </trim>
    </select>

    <select id="getTableColumnInfo" resultType="map">
        select
            COLUMN_NAME,
            COLUMN_COMMENT,
            DATA_TYPE,
            CHARACTER_MAXIMUM_LENGTH,
            IS_NULLABLE,
            COLUMN_DEFAULT,
            COLUMN_KEY
        FROM
            INFORMATION_SCHEMA.COLUMNS
        WHERE
            TABLE_SCHEMA = #{dataName}
            and
            TABLE_NAME = #{tableName};
    </select>
</mapper>

